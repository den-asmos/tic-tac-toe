export const selectField = (state) => state.field;
export const selectMove = (state) => state.move;
export const selectWinner = (state) => state.winner;
export const selectFull = (state) => state.full;
